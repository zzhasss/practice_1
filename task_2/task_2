#include <iostream>

int main() {
    int N, M;
    std::cin >> N >> M;

    for (int i = 0; i < M; i++) {
        int u, v;
        std::cin >> u >> v;
    }

    std::cout << (N - 1 - M);

    return 0;
}